---
export const prerender = false;
import Layout from "../layouts/Layout.astro";
import NavBar from "../components/NavBar.astro";
import TopTag from "../components/TopTag.astro";

let historial = [];

import { SERVER_BACKEND_URL } from "../config";
const backendUrl = SERVER_BACKEND_URL;
const token = Astro.cookies.get("auth_token")?.value;
const headers = {
	"Content-Type": "application/json",
	...(token && { Cookie: `auth_token=${token}` }),
};

const { Agent } = await import("node:https");
const agent = new Agent({ rejectUnauthorized: false });

try {
	// @ts-ignore
	// @ts-ignore
	const response = await fetch(`${backendUrl}/api/v1/HistorialClinico`, {
		headers,
		agent,
	} as any);
	if (response.ok) {
		historial = await response.json();
	} else {
		console.error("Error fetching historial:", response.status);
	}
} catch (error) {
	console.error("Error fetching historial:", error);
}
---

<Layout title="Historial de Citas">
	<div
		class="flex h-screen overflow-hidden bg-gradient-to-br from-[#F0F4F8] via-[#E6F0EA] to-[#F0F4F8]"
	>
		<NavBar />

		<div class="flex-1 flex flex-col min-w-0 overflow-hidden relative">
			<!-- Background Elements -->
			<div
				class="absolute top-[-10%] right-[-5%] w-96 h-96 bg-[#2F5D50]/10 rounded-full blur-3xl pointer-events-none"
			>
			</div>
			<div
				class="absolute bottom-[-10%] left-[-5%] w-96 h-96 bg-[#2F5D50]/5 rounded-full blur-3xl pointer-events-none"
			>
			</div>

			<TopTag titulo="Historial Clínico" nombreUsuario="Administrador" />

			<main
				class="flex-1 overflow-y-auto p-4 sm:p-6 lg:p-8 scrollbar-thin scrollbar-thumb-[#2F5D50]/20 scrollbar-track-transparent relative z-10"
			>
				<!-- Tabla Container -->
				<div
					class="glass-panel rounded-3xl overflow-hidden animate-fade-in-up stagger-2 border border-white/40 shadow-xl flex flex-col h-full max-h-[calc(100vh-140px)]"
				>
					<!-- Header de la tabla con Filtros -->
					<div
						class="p-6 border-b border-[#2F5D50]/10 bg-white/40 flex flex-col sm:flex-row justify-between items-center gap-4"
					>
						<h2
							class="text-xl font-bold text-[#2F5D50] flex items-center gap-2"
						>
							<i class="fa-solid fa-file-medical"></i>
							Registros
						</h2>

						<div
							class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto"
						>
							<!-- Buscador -->
							<div class="relative group w-full sm:w-64">
								<input
									type="text"
									id="searchInput"
									placeholder="Buscar paciente, teléfono, fisio..."
									class="glass-input w-full rounded-xl pl-10 pr-4 py-2 text-sm focus:ring-2 focus:ring-[#2F5D50]/20 outline-none transition-all"
								/>
								<i
									class="fa-solid fa-search absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-[#2F5D50] transition-colors"
								></i>
							</div>

							<!-- Selector de Registros por página -->
							<div
								class="flex items-center gap-2 bg-white/50 px-3 py-2 rounded-xl border border-white/60"
							>
								<span
									class="text-xs font-bold text-gray-500 whitespace-nowrap"
									>Mostrar:</span
								>
								<select
									id="rowsPerPage"
									class="bg-transparent text-sm font-bold text-[#2F5D50] outline-none cursor-pointer"
								>
									<option value="5">5</option>
									<option value="10" selected>10</option>
								</select>
							</div>
						</div>
					</div>

					<!-- Tabla -->
					<div class="overflow-auto custom-scrollbar flex-1 relative">
						<table class="w-full text-left border-collapse">
							<thead
								class="sticky top-0 z-10 bg-white/95 backdrop-blur-md shadow-sm"
							>
								<tr
									class="text-[#2F5D50] border-b border-[#2F5D50]/10 text-sm"
								>
									<th class="p-4 font-bold whitespace-nowrap"
										>Paciente</th
									>
									<th class="p-4 font-bold whitespace-nowrap"
										>Fecha</th
									>
									<th class="p-4 font-bold whitespace-nowrap"
										>Hora</th
									>
									<th
										class="p-4 font-bold whitespace-nowrap w-1/3"
										>Servicio & Motivo</th
									>
									<th
										class="p-4 font-bold whitespace-nowrap text-center"
										>Estado</th
									>
									<th
										class="p-4 font-bold whitespace-nowrap text-center"
										>Paquete</th
									>
								</tr>
							</thead>
							<tbody
								id="tableBody"
								class="divide-y divide-[#2F5D50]/5"
							>
								<!-- Contenido inyectado por JS -->
							</tbody>
						</table>

						<!-- Estado Vacío (oculto por defecto) -->
						<div
							id="emptyState"
							class="hidden absolute inset-0 flex flex-col items-center justify-center text-gray-500 bg-white/50 backdrop-blur-sm"
						>
							<i
								class="fa-regular fa-folder-open text-4xl mb-2 opacity-50"
							></i>
							<p>No se encontraron registros</p>
						</div>
					</div>

					<!-- Paginación -->
					<div
						class="p-4 border-t border-[#2F5D50]/10 bg-white/40 flex justify-between items-center"
					>
						<span
							class="text-xs sm:text-sm text-gray-600 font-medium"
						>
							Mostrando <span
								id="showingStart"
								class="font-bold text-[#2F5D50]">0</span
							> - <span
								id="showingEnd"
								class="font-bold text-[#2F5D50]">0</span
							> de <span
								id="totalRecords"
								class="font-bold text-[#2F5D50]">0</span
							>
						</span>

						<div class="flex gap-2">
							<button
								id="prevBtn"
								class="w-9 h-9 rounded-lg flex items-center justify-center bg-white/60 text-[#2F5D50] hover:bg-[#2F5D50] hover:text-white transition-all disabled:opacity-50 disabled:cursor-not-allowed shadow-sm border border-white/50"
							>
								<i class="fa-solid fa-chevron-left text-xs"></i>
							</button>
							<div id="pageNumbers" class="flex gap-1">
								<!-- Números de página generados dinámicamente -->
							</div>
							<button
								id="nextBtn"
								class="w-9 h-9 rounded-lg flex items-center justify-center bg-white/60 text-[#2F5D50] hover:bg-[#2F5D50] hover:text-white transition-all disabled:opacity-50 disabled:cursor-not-allowed shadow-sm border border-white/50"
							>
								<i class="fa-solid fa-chevron-right text-xs"
								></i>
							</button>
						</div>
					</div>
				</div>
			</main>
		</div>
	</div>

	<!-- Modal Detalle Clínico -->
	<div
		id="modalDetail"
		class="fixed inset-0 z-50 hidden"
		aria-labelledby="modal-title"
		role="dialog"
		aria-modal="true"
	>
		<!-- Backdrop -->
		<div
			class="fixed inset-0 bg-gray-900/60 backdrop-blur-sm transition-opacity opacity-0"
			id="modalBackdrop"
		>
		</div>

		<!-- Modal Panel -->
		<div class="fixed inset-0 z-10 overflow-y-auto">
			<div
				class="flex min-h-full items-center justify-center p-4 text-center sm:p-0"
			>
				<div
					id="modalPanel"
					class="relative transform overflow-hidden rounded-3xl bg-white text-left shadow-2xl transition-all sm:my-8 w-full max-w-4xl opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
				>
					<!-- Header -->
					<div
						class="bg-[#2F5D50] px-6 py-4 flex justify-between items-center"
					>
						<h3
							class="text-lg font-bold text-white flex items-center gap-2"
							id="modal-title"
						>
							<i class="fa-solid fa-file-medical-alt"></i> Detalle Clínico
							Integral
						</h3>
						<button
							id="closeModalBtn"
							type="button"
							class="text-white/80 hover:text-white transition-colors rounded-full p-1 hover:bg-white/10"
						>
							<i class="fa-solid fa-xmark text-xl"></i>
						</button>
					</div>

					<!-- Body -->
					<div
						class="px-6 py-6 max-h-[calc(100vh-140px)] overflow-y-auto custom-scrollbar"
					>
						<!-- Loading State -->
						<div
							id="modalLoading"
							class="hidden flex flex-col items-center justify-center py-12"
						>
							<i
								class="fa-solid fa-circle-notch fa-spin text-4xl text-[#2F5D50] mb-3"
							></i>
							<p class="text-gray-500 font-medium animate-pulse">
								Cargando expediente...
							</p>
						</div>

						<!-- Content -->
						<div id="modalContent" class="hidden space-y-8">
							<!-- 1. Patient & Session Header -->
							<div
								class="flex flex-col sm:flex-row gap-6 items-start border-b border-gray-100 pb-6"
							>
								<!-- Patient Card -->
								<div
									class="flex items-center gap-4 bg-gray-50/50 p-4 rounded-2xl border border-gray-100 w-full sm:w-auto flex-1"
								>
									<div
										class="w-16 h-16 rounded-full bg-white border-2 border-[#2F5D50]/20 shadow-sm flex items-center justify-center text-2xl font-bold text-[#2F5D50] overflow-hidden"
										id="detailPhotoContainer"
									>
										<i class="fa-solid fa-user"></i>
									</div>
									<div>
										<h4
											class="text-lg font-bold text-gray-900"
											id="detailPatientName"
										>
											Paciente
										</h4>
										<div class="flex flex-wrap gap-3 mt-1">
											<span
												class="text-xs font-medium text-gray-500 bg-white px-2 py-0.5 rounded-md border border-gray-200"
											>
												<i
													class="fa-solid fa-cake-candles text-[#2F5D50] mr-1"
												></i>
												<span id="detailAge">--</span> años
											</span>
											<span
												class="text-xs font-medium text-gray-500 bg-white px-2 py-0.5 rounded-md border border-gray-200"
											>
												<i
													class="fa-solid fa-phone text-[#2F5D50] mr-1"
												></i>
												<span id="detailPhone">--</span>
											</span>
										</div>
									</div>
								</div>

								<!-- Session Info -->
								<div
									class="flex-1 w-full sm:w-auto grid grid-cols-2 gap-3"
								>
									<div
										class="bg-blue-50/50 p-3 rounded-xl border border-blue-100"
									>
										<p
											class="text-[10px] uppercase font-bold text-blue-800 mb-0.5 tracking-wider"
										>
											Fecha
										</p>
										<p
											class="text-sm font-semibold text-gray-700"
											id="detailDate"
										>
											--
										</p>
									</div>
									<div
										class="bg-indigo-50/50 p-3 rounded-xl border border-indigo-100"
									>
										<p
											class="text-[10px] uppercase font-bold text-indigo-800 mb-0.5 tracking-wider"
										>
											Hora
										</p>
										<p
											class="text-sm font-semibold text-gray-700"
											id="detailTime"
										>
											--
										</p>
									</div>
									<div
										class="col-span-2 bg-emerald-50/50 p-3 rounded-xl border border-emerald-100 flex items-center gap-3"
									>
										<div
											class="w-8 h-8 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600"
										>
											<i
												class="fa-solid fa-user-doctor text-xs"
											></i>
										</div>
										<div>
											<p
												class="text-[10px] uppercase font-bold text-emerald-800 mb-0.5 tracking-wider"
											>
												Fisioterapeuta
											</p>
											<p
												class="text-sm font-semibold text-gray-700 truncate"
												id="detailPhysio"
											>
												--
											</p>
										</div>
									</div>
								</div>
							</div>

							<!-- 2. Clinical Data Grid -->
							<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
								<!-- Left Col: Evaluation & Notes -->
								<div class="space-y-6">
									<!-- Therapeutic Note -->
									<div
										class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden"
									>
										<div
											class="bg-gray-50 px-4 py-2 border-b border-gray-200 flex justify-between items-center"
										>
											<h4
												class="font-bold text-gray-700 text-sm flex items-center gap-2"
											>
												<i
													class="fa-solid fa-pen-to-square text-orange-500"
												></i> Nota Terapéutica
											</h4>
										</div>
										<div class="p-4 relative">
											<i
												class="fa-solid fa-quote-left text-4xl text-gray-100 absolute top-2 left-2 -z-0"
											></i>
											<p
												class="text-sm text-gray-700 leading-relaxed italic relative z-10"
												id="detailNote"
											>
												Sin nota registrada.
											</p>
										</div>
									</div>

									<!-- EVA & Diagnostics -->
									<div class="grid grid-cols-2 gap-4">
										<!-- EVA Scale -->
										<div
											class="bg-white rounded-2xl shadow-sm border border-gray-200 p-4 flex flex-col items-center justify-center relative overflow-hidden"
											id="detailEvaCard"
										>
											<div
												class="absolute inset-0 opacity-10 bg-current"
											>
											</div>
											<span
												class="text-[10px] font-bold uppercase tracking-widest mb-1 z-10"
												>Escala EVA</span
											>
											<span
												class="text-4xl font-black z-10"
												id="detailEvaVal">--</span
											>
											<span
												class="text-[10px] font-bold uppercase mt-1 opacity-70 z-10"
												id="detailEvaLabel">--</span
											>
										</div>

										<!-- Diagnostic (if available) -->
										<div
											class="bg-indigo-50 rounded-2xl shadow-sm border border-indigo-100 p-4 flex flex-col justify-center"
										>
											<span
												class="text-[10px] font-bold text-indigo-800 uppercase tracking-widest mb-1 flex items-center gap-1"
											>
												<i
													class="fa-solid fa-stethoscope"
												></i> Diagnóstico
											</span>
											<p
												class="text-xs font-semibold text-gray-800 leading-snug"
												id="detailDiagnostic"
											>
												--
											</p>
										</div>
									</div>

									<!-- Full Evaluation (Collapsible or Block) -->
									<div
										id="detailEvaluationBlock"
										class="hidden bg-white rounded-2xl shadow-sm border border-gray-200 p-4 space-y-3"
									>
										<h5
											class="text-xs font-bold text-gray-400 uppercase tracking-wider border-b border-gray-100 pb-2"
										>
											Detalles de Evaluación
										</h5>
										<div
											class="grid grid-cols-2 gap-3 text-xs"
										>
											<div>
												<p
													class="font-bold text-gray-500"
												>
													Mecanismo de Lesión:
												</p>
												<p
													class="text-gray-700"
													id="evalInjury"
												>
													--
												</p>
											</div>
											<div>
												<p
													class="font-bold text-gray-500"
												>
													Inicio del Dolor:
												</p>
												<p
													class="text-gray-700"
													id="evalOnset"
												>
													--
												</p>
											</div>
											<div class="col-span-2">
												<p
													class="font-bold text-gray-500"
												>
													Factores Agravantes:
												</p>
												<p
													class="text-gray-700"
													id="evalAggravating"
												>
													--
												</p>
											</div>
										</div>
									</div>
								</div>

								<!-- Right Col: Services & Finance -->
								<div class="space-y-6">
									<!-- Package Status -->
									<div
										id="detailPackageBlock"
										class="hidden bg-gradient-to-r from-purple-50 to-pink-50 rounded-2xl p-5 border border-purple-100 relative overflow-hidden"
									>
										<i
											class="fa-solid fa-layer-group absolute -right-4 -bottom-4 text-6xl text-purple-200/50 transform -rotate-12"
										></i>
										<div class="relative z-10">
											<div
												class="flex justify-between items-center mb-3"
											>
												<h4
													class="font-bold text-purple-900 text-sm"
												>
													Progreso del Paquete
												</h4>
												<span
													class="bg-white/60 px-2 py-0.5 rounded-md text-[10px] font-bold text-purple-700 border border-purple-200"
													id="detailPackageLabel"
													>Sesión X de Y</span
												>
											</div>
											<div
												class="w-full bg-white/60 h-2 rounded-full overflow-hidden border border-purple-100"
											>
												<div
													class="bg-purple-500 h-full rounded-full transition-all duration-1000"
													id="detailPackageBar"
													style="width: 0%"
												>
												</div>
											</div>
										</div>
									</div>

									<!-- Services Table -->
									<div
										class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden"
									>
										<div
											class="bg-gray-50 px-5 py-3 border-b border-gray-200"
										>
											<h4
												class="font-bold text-gray-700 text-sm"
											>
												Servicios y Costos
											</h4>
										</div>
										<table class="w-full text-left text-xs">
											<tbody
												class="divide-y divide-gray-50"
												id="detailServicesTable"
											>
												<!-- Dynamic -->
											</tbody>
											<tfoot
												class="bg-gray-50/50 border-t border-gray-100 font-bold text-gray-700"
											>
												<tr>
													<td
														class="px-5 py-3 text-right"
														colspan="2">Total</td
													>
													<td
														class="px-5 py-3 text-right"
														id="detailTotal"
														>$0.00</td
													>
												</tr>
											</tfoot>
										</table>
									</div>

									<!-- Fotos de la cita -->
									<div
										id="detailPhotosBlock"
										class="hidden bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden"
									>
										<div
											class="bg-gray-50 px-5 py-3 border-b border-gray-200"
										>
											<h4
												class="font-bold text-gray-700 text-sm"
											>
												Fotografías de la sesión
											</h4>
										</div>
										<div
											id="detailPhotosGrid"
											class="p-4 grid grid-cols-2 sm:grid-cols-4 gap-3"
										>
										</div>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

	<!-- Visor de foto ampliada -->
	<div
		id="photoViewer"
		class="hidden fixed inset-0 z-[70] bg-black/80 backdrop-blur-sm flex items-center justify-center p-6 cursor-zoom-out"
		onclick="this.classList.add('hidden')"
	>
		<img
			id="photoViewerImg"
			src=""
			alt="Foto de la sesión"
			class="max-w-full max-h-full rounded-2xl shadow-2xl object-contain"
		/>
	</div>

	<!-- Script Cliente para Paginación y Filtro -->
	<script define:vars={{ historial, backendUrl }}>
		// Datos iniciales desde el servidor
		let allData = historial || [];
		// Ensure allData is an array
		if (!Array.isArray(allData) && allData.body) {
			allData = allData.body;
		} else if (!Array.isArray(allData)) {
			allData = [];
		}

		let filteredData = [...allData];

		// Estado de la paginación
		let currentPage = 1;
		let rowsPerPage = 10;

		// Elementos DOM
		const tableBody = document.getElementById("tableBody");
		const emptyState = document.getElementById("emptyState");
		const searchInput = document.getElementById("searchInput");
		const rowsPerPageSelect = document.getElementById("rowsPerPage");
		const prevBtn = document.getElementById("prevBtn");
		const nextBtn = document.getElementById("nextBtn");
		const showingStart = document.getElementById("showingStart");
		const showingEnd = document.getElementById("showingEnd");
		const totalRecords = document.getElementById("totalRecords");

		// Modal Elements
		const modalDetail = document.getElementById("modalDetail");
		const modalBackdrop = document.getElementById("modalBackdrop");
		const modalPanel = document.getElementById("modalPanel");
		const closeModalBtn = document.getElementById("closeModalBtn");
		const modalLoading = document.getElementById("modalLoading");
		const modalContent = document.getElementById("modalContent");

		const formatDate = (dateString, timeString) => {
			if (!dateString) return "-";
			// console.log("Formatting Date:", dateString, typeof dateString);

			let d;
			// Check if it's YYYY-MM-DD or starts with it (like ISO)
			if (
				typeof dateString === "string" &&
				/^\d{4}-\d{2}-\d{2}/.test(dateString)
			) {
				const [y, m, day] = dateString
					.substring(0, 10)
					.split("-")
					.map(Number);
				d = new Date(y, m - 1, day);
			} else {
				d = new Date(dateString);
			}

			const dateStr = d.toLocaleDateString("es-MX", {
				day: "2-digit",
				month: "2-digit",
				year: "numeric",
			});

			let timeStr = "";
			if (timeString) {
				const [h, m] = timeString.split(":");
				timeStr = `${h}:${m}`;
			}
			return `${dateStr} ${timeStr}`;
		};

		// --- Modal Logic ---
		function toggleModal(show) {
			if (show) {
				modalDetail.classList.remove("hidden");
				// Animate in
				setTimeout(() => {
					modalBackdrop.classList.remove("opacity-0");
					modalPanel.classList.remove(
						"opacity-0",
						"translate-y-4",
						"sm:translate-y-0",
						"sm:scale-95",
					);
				}, 10);
			} else {
				// Animate out
				modalBackdrop.classList.add("opacity-0");
				modalPanel.classList.add(
					"opacity-0",
					"translate-y-4",
					"sm:translate-y-0",
					"sm:scale-95",
				);
				setTimeout(() => {
					modalDetail.classList.add("hidden");
				}, 300);
			}
		}

		closeModalBtn.addEventListener("click", () => toggleModal(false));
		modalBackdrop.addEventListener("click", () => toggleModal(false));

		async function openDetailModal(id) {
			toggleModal(true);
			modalLoading.classList.remove("hidden");
			modalContent.classList.add("hidden");

			try {
				// Determine endpoint based on known patterns or user hint.
				// Assuming /api/v1/Citas/detalle/:id or similar.
				// Using a fallback/test URL or the most likely one.
				// Vía el proxy /api del front para que viaje la cookie de sesión.
				const response = await fetch(
					`/api/v1/Agenda/${id}`,
				);
				if (!response.ok) throw new Error("Error fetching details");

				const json = await response.json();
				// Support array wrapper if present
				const data = Array.isArray(json) ? json[0] : json;
				// Handle both potential keys or fallback
				const detail =
					data.get_appointment_full_detail ||
					data.get_appointment_detail;

				if (detail) {
					populateModal(detail);
					loadAppointmentPhotos(id);
					modalLoading.classList.add("hidden");
					modalContent.classList.remove("hidden");
				} else {
					console.error("Invalid payload structure", data);
					alert("Error: Estructura de datos inválida");
					toggleModal(false);
				}
			} catch (error) {
				console.error(error);
				alert("No se pudo cargar el detalle de la cita.");
				toggleModal(false);
			}
		}

		async function loadAppointmentPhotos(appointmentId) {
			const block = document.getElementById("detailPhotosBlock");
			const grid = document.getElementById("detailPhotosGrid");
			block.classList.add("hidden");
			grid.innerHTML = "";
			try {
				const res = await fetch(`/api/v1/Citas/${appointmentId}/photos`);
				if (!res.ok) return;
				const photos = await res.json();
				if (!Array.isArray(photos) || photos.length === 0) return;
				photos.forEach((photo) => {
					const url = `${backendUrl}/${photo.path.replace(/^\//, "")}`;
					const img = document.createElement("img");
					img.src = url;
					img.alt = "Foto de la sesión";
					img.loading = "lazy";
					img.className =
						"w-full h-28 object-cover rounded-xl border border-gray-100 cursor-zoom-in hover:opacity-90 transition-opacity";
					img.onclick = () => {
						document.getElementById("photoViewerImg").src = url;
						document
							.getElementById("photoViewer")
							.classList.remove("hidden");
					};
					grid.appendChild(img);
				});
				block.classList.remove("hidden");
			} catch (err) {
				console.error("Error loading appointment photos:", err);
			}
		}

		function populateModal(data) {
			const p = data.patient || {};
			const a = data.appointment || {};
			const emp = data.employed || {};
			const pkg = data.package || {};
			const ev = data.evaluation || {};
			const sn = data.notes || data.session_notes || {}; // 'notes' in actual payload
			const services = data.services || [];
			const ctx = data.context || {};

			// 1. Patient Info
			const pName =
				p.full_name ||
				`${p.firstname || ""} ${p.lastname || ""} ${p.motherlastname || ""}`.trim();
			document.getElementById("detailPatientName").textContent =
				pName || "Sin nombre";

			// Age/Phone (Check if keys exist, otherwise generic)
			let age = p.age;
			if (!age && p.birthday) {
				const birthDate = new Date(p.birthday);
				const today = new Date();
				let calculatedAge =
					today.getFullYear() - birthDate.getFullYear();
				const m = today.getMonth() - birthDate.getMonth();
				if (
					m < 0 ||
					(m === 0 && today.getDate() < birthDate.getDate())
				) {
					calculatedAge--;
				}
				age = calculatedAge;
			}
			document.getElementById("detailAge").textContent = age || "--";
			document.getElementById("detailPhone").textContent =
				p.phone || "--";

			// Photo (if available) - for now just Initial
			const init = (pName || "?").charAt(0).toUpperCase();
			// Photo Logic (Matches dashboardEmpleado.astro)
			const rawPhoto = p.photo;
			const hasPhoto =
				rawPhoto &&
				rawPhoto !== "default.png" &&
				!String(rawPhoto).includes("default.png");

			let photoHtml = "";
			if (hasPhoto) {
				let finalUrl = rawPhoto;
				if (!finalUrl.startsWith("http")) {
					if (
						finalUrl.startsWith("uploads") ||
						finalUrl.startsWith("/uploads")
					) {
						finalUrl = `${backendUrl}/${finalUrl.startsWith("/") ? finalUrl.slice(1) : finalUrl}`;
					} else {
						// Fallback assuming it's just a filename in standard folder
						finalUrl = `${backendUrl}/uploads/images/${finalUrl}`;
					}
				}
				const fallbackHtml = `<span class='text-2xl'>${init}</span>`;
				photoHtml = `<img src="${finalUrl}" class="w-full h-full object-cover" onerror="this.parentNode.innerHTML=\`${fallbackHtml}\`">`;
			} else {
				photoHtml = `<span class="text-2xl">${init}</span>`;
			}

			document.getElementById("detailPhotoContainer").innerHTML =
				photoHtml;

			// 2. Session Info
			// Date: 'currentday' or 'date'
			const dateRaw = a.currentday || a.date;
			const d = new Date(dateRaw);
			// Format YYYY-MM-DD -> DD/MM/YYYY
			const dateParts = (dateRaw || "").split("-");
			const dateFormatted =
				dateParts.length === 3
					? `${dateParts[2]}/${dateParts[1]}/${dateParts[0]}`
					: dateRaw;

			document.getElementById("detailDate").textContent =
				dateFormatted || "--";

			// Time: 'inithour' or 'start_time'
			const timeRaw = a.inithour || a.start_time || "";
			document.getElementById("detailTime").textContent =
				timeRaw.substring(0, 5) || "--";

			// Physio: 'firstname' + 'lastname'
			const empName =
				emp.full_name ||
				`${emp.firstname || ""} ${emp.lastname || ""}`.trim();
			document.getElementById("detailPhysio").textContent =
				empName || "No asignado";

			// 3. Clinical Data
			document.getElementById("detailNote").textContent =
				sn.therapeuticnote ||
				sn.therapeutic_note ||
				"Sin nota registrada.";

			// Diagnostics
			const diag =
				ev?.diagnostic ||
				ctx?.diagnostic_inherited ||
				a.reason ||
				"Sin diagnóstico"; // Fallback to reason if no diag
			document.getElementById("detailDiagnostic").textContent = diag;

			// EVA Logic
			const evaVal = sn.pain_intensity ?? ev?.pain_intensity;
			const evaCard = document.getElementById("detailEvaCard");
			const evaLabel = document.getElementById("detailEvaLabel");

			if (evaVal !== null && evaVal !== undefined) {
				const val = parseInt(evaVal);
				document.getElementById("detailEvaVal").textContent = val;

				let bgClass = "bg-white",
					borderClass = "border-gray-200",
					textClass = "text-gray-900";
				let label = "Sin Dolor";

				if (val >= 1 && val <= 3) {
					bgClass = "bg-green-50";
					borderClass = "border-green-200";
					textClass = "text-green-700";
					label = "Leve";
				} else if (val >= 4 && val <= 6) {
					bgClass = "bg-yellow-50";
					borderClass = "border-yellow-200";
					textClass = "text-yellow-700";
					label = "Moderado";
				} else if (val >= 7) {
					bgClass = "bg-red-50";
					borderClass = "border-red-200";
					textClass = "text-red-700";
					label = "Intenso";
				}

				evaCard.className = `rounded-2xl shadow-sm border p-4 flex flex-col items-center justify-center relative overflow-hidden transition-colors duration-300 ${bgClass} ${borderClass} ${textClass}`;
				evaLabel.textContent = label;
			} else {
				document.getElementById("detailEvaVal").textContent = "--";
				evaLabel.textContent = "N/A";
				evaCard.className =
					"bg-gray-50 rounded-2xl shadow-sm border border-gray-200 p-4 flex flex-col items-center justify-center relative overflow-hidden text-gray-400";
			}

			// Evaluation Data (Show only if exists and has useful data)
			if (ev && ev.exists && (ev.injury_mechanism || ev.pain_onset)) {
				document
					.getElementById("detailEvaluationBlock")
					.classList.remove("hidden");
				document.getElementById("evalInjury").textContent =
					ev.injury_mechanism || "No registrado";
				document.getElementById("evalOnset").textContent =
					ev.pain_onset || "No registrado";
				document.getElementById("evalAggravating").textContent =
					ev.pain_aggravating_factors || "No registrado";
			} else {
				document
					.getElementById("detailEvaluationBlock")
					.classList.add("hidden");
			}

			// 4. Services & Costs
			const table = document.getElementById("detailServicesTable");
			table.innerHTML = "";
			let grandTotal = 0;

			services.forEach((svc) => {
				const tr = document.createElement("tr");
				tr.className = "hover:bg-gray-50/50";
				tr.innerHTML = `
					<td class="px-5 py-3 font-medium text-gray-700">
                        ${svc.name || svc.nombre_servicio || "Servicio"}
                        ${svc.type === "additional" ? '<span class="text-[9px] bg-yellow-100 text-yellow-700 px-1.5 py-0.5 rounded ml-1">ADICIONAL</span>' : ""}
                    </td>
					<td class="px-5 py-3 text-center text-gray-500">x${svc.amount || 1}</td>
					<td class="px-5 py-3 text-right font-medium text-gray-900">$${Number(svc.subtotal || svc.unit_price || 0).toFixed(2)}</td>
				`;
				table.appendChild(tr);
				grandTotal += Number(svc.subtotal || svc.unit_price || 0);
			});

			document.getElementById("detailTotal").textContent =
				`$${grandTotal.toFixed(2)}`;

			// 5. Package Logic
			// Payload: { session_number: 3, total_sessions: 5, package_price: 2200 }
			// Or: { has_package: true, ... }
			const hasPkg = pkg.total_sessions > 0 || pkg.has_package;

			const pkgBlock = document.getElementById("detailPackageBlock");
			if (hasPkg) {
				pkgBlock.classList.remove("hidden");
				const current =
					pkg.session_number ||
					pkg.current_session ||
					pkg.sessions_used ||
					0;
				const total = pkg.total_sessions;
				const pct = Math.min((current / total) * 100, 100);

				document.getElementById("detailPackageLabel").textContent =
					`Sesión ${current} de ${total}`;
				document.getElementById("detailPackageBar").style.width =
					`${pct}%`;
			} else {
				pkgBlock.classList.add("hidden");
			}
		}

		// --- End Modal Logic ---

		// Función Principal de Renderizado
		function renderTable() {
			// Calcular índices
			const startIndex = (currentPage - 1) * rowsPerPage;
			const endIndex = Math.min(
				startIndex + rowsPerPage,
				filteredData.length,
			);
			const pageData = filteredData.slice(startIndex, endIndex);

			// Limpiar tabla
			tableBody.innerHTML = "";

			if (pageData.length === 0) {
				emptyState.classList.remove("hidden");
				showingStart.textContent = 0;
				showingEnd.textContent = 0;
				totalRecords.textContent = 0;
				updatePaginationControls();
				return;
			}

			emptyState.classList.add("hidden");

			// Generar filas
			pageData.forEach((item) => {
				const id = item.id;
				const patientName = item.patientname || "Desconocido";
				const dateRaw = item.currentday; // ISO String
				const timeRaw = item.inithour;
				const serviceName = item.name || "Servicio General";
				const reason = item.reason || "";
				const status = item.status;
				const hasPackage =
					item.has_package === true || item.has_package === "true";

				// Determine Styles based on Service Name
				const isValuation = serviceName
					.toLowerCase()
					.normalize("NFD")
					.replace(/[\u0300-\u036f]/g, "")
					.includes("valoracion");

				const row = document.createElement("tr");

				const valStyles =
					"bg-violet-50 border-l-[4px] border-l-violet-500 hover:bg-violet-100/80 cursor-pointer";
				const stdStyles =
					"bg-white border-l-[4px] border-l-transparent hover:bg-gray-50 border-b border-gray-100/50 cursor-pointer";

				row.className = `transition-all duration-200 group text-sm text-gray-700 ${isValuation ? valStyles : stdStyles}`;

				// Add Click Handler
				row.onclick = () => {
					if (id) openDetailModal(id);
					else console.warn("No ID found for item", item);
				};

				const initial = patientName.charAt(0).toUpperCase();

				// Format Date and Time separately
				const formattedDate = formatDate(dateRaw, null).trim(); // Just date
				const formattedTime = timeRaw
					? timeRaw.substring(0, 5)
					: "--:--";

				// Enhanced Service Badge
				let serviceBadgeClass =
					"bg-gray-100 text-gray-700 border-gray-200";
				let icon = "fa-notes-medical";

				if (isValuation) {
					serviceBadgeClass =
						"bg-indigo-100 text-indigo-700 border-indigo-200";
					icon = "fa-user-doctor";
				} else if (serviceName.toLowerCase().includes("terapia")) {
					serviceBadgeClass =
						"bg-teal-100 text-teal-700 border-teal-200";
					icon = "fa-hand-holding-medical";
				} else if (serviceName.toLowerCase().includes("descarga")) {
					serviceBadgeClass =
						"bg-orange-100 text-orange-700 border-orange-200";
					icon = "fa-bolt";
				}

				const serviceHtml = `
                    <div class="flex items-center gap-2">
                        <span class="px-3 py-1.5 rounded-lg border ${serviceBadgeClass} font-bold text-xs flex items-center gap-2 shadow-sm">
                            <i class="fa-solid ${icon}"></i>
                            ${serviceName}
                        </span>
                    </div>
                `;

				// Status Badge
				let statusBadge = "";
				let statusColor = "bg-gray-100 text-gray-600 border-gray-200";
				let statusLabel = status || "Desconocido";

				if (status === "wait") {
					statusLabel = "En Espera";
					statusColor =
						"bg-yellow-50 text-yellow-700 border-yellow-200";
				}
				if (status === "confirmed") {
					statusLabel = "Confirmada";
					statusColor = "bg-blue-50 text-blue-700 border-blue-200";
				}
				if (status === "concluded") {
					statusLabel = "Concluida";
					statusColor = "bg-green-50 text-green-700 border-green-200";
				}
				if (status === "canceled") {
					statusLabel = "Cancelada";
					statusColor = "bg-red-50 text-red-700 border-red-200";
				}
				if (status === "no assisted") {
					statusLabel = "No Asistió";
					statusColor = "bg-red-50 text-red-700 border-red-200";
				}

				statusBadge = `<span class="px-2.5 py-1 rounded-md text-[10px] font-bold uppercase tracking-wider border ${statusColor}">${statusLabel}</span>`;

				// Package Cell
				const packageCell = hasPackage
					? `<div class="flex justify-center">
                           <div class="w-8 h-8 rounded-full bg-purple-100 flex items-center justify-center text-purple-600 border border-purple-200 shadow-sm" title="Paquete Activo">
                                <i class="fa-solid fa-box-open text-xs"></i>
                           </div>
                       </div>`
					: `<div class="flex justify-center"><span class="text-gray-300">-</span></div>`;

				row.innerHTML = `
                    <!-- Paciente -->
                    <td class="p-4 align-middle">
                        <div class="flex items-center gap-3">
                             <div class="w-9 h-9 rounded-full bg-[#2F5D50]/10 flex items-center justify-center text-[#2F5D50] font-bold text-sm shadow-sm flex-shrink-0">
                                ${initial}
                            </div>
                            <div>
                                <h4 class="font-bold text-gray-800 text-sm leading-tight">${patientName}</h4>
                            </div>
                        </div>
                    </td>

                    <!-- Fecha -->
                    <td class="p-4 align-middle whitespace-nowrap">
                        <div class="flex items-center gap-2 text-gray-700 font-medium text-sm">
                            <i class="fa-regular fa-calendar text-[#2F5D50]/60"></i>
                            ${formattedDate}
                        </div>
                    </td>

                    <!-- Hora -->
                    <td class="p-4 align-middle whitespace-nowrap">
                        <div class="flex items-center gap-2 text-gray-700 font-medium text-sm">
                            <i class="fa-regular fa-clock text-[#2F5D50]/60"></i>
                            ${formattedTime}
                        </div>
                    </td>

                    <!-- Servicio y Nota -->
                    <td class="p-4 align-middle">
                        <div class="flex flex-col gap-2">
                            ${serviceHtml}
                            ${reason ? `<div class="text-xs text-gray-500 italic pl-1 border-l-2 border-gray-200 line-clamp-2" title="${reason}">${reason}</div>` : ""}
                        </div>
                    </td>

                    <!-- Estado -->
                    <td class="p-4 align-middle text-center">
                        ${statusBadge}
                    </td>

                    <!-- Paquete -->
                    <td class="p-4 align-middle text-center">
                        ${packageCell}
                    </td>
                `;

				tableBody.appendChild(row);
			});

			// Actualizar contadores
			showingStart.textContent =
				filteredData.length > 0 ? startIndex + 1 : 0;
			showingEnd.textContent = endIndex;
			totalRecords.textContent = filteredData.length;

			updatePaginationControls();
		}

		function updatePaginationControls() {
			const totalPages = Math.ceil(filteredData.length / rowsPerPage);

			prevBtn.disabled = currentPage === 1;
			nextBtn.disabled = currentPage === totalPages || totalPages === 0;
		}

		// Event Listeners
		searchInput.addEventListener("input", (e) => {
			const term = e.target.value.trim().toLowerCase();
			// Solo dígitos del término, para comparar contra teléfonos sin
			// importar el formato (espacios, guiones, paréntesis, etc.)
			const digits = term.replace(/\D/g, "");

			filteredData = allData.filter((item) => {
				const patient = (item.patientname || "").toLowerCase();
				const service = (item.name || "").toLowerCase();
				const phone = String(
					item.patientphone ||
						item.phone ||
						item.patient?.phone ||
						"",
				).replace(/\D/g, "");

				const coincidePatient = patient.includes(term);
				const coincideService = service.includes(term);
				const coincidePhone = digits.length > 0 && phone.includes(digits);

				return coincidePatient || coincideService || coincidePhone;
			});
			currentPage = 1; // Resetear a primera página
			renderTable();
		});

		rowsPerPageSelect.addEventListener("change", (e) => {
			rowsPerPage = parseInt(e.target.value);
			currentPage = 1;
			renderTable();
		});

		prevBtn.addEventListener("click", () => {
			if (currentPage > 1) {
				currentPage--;
				renderTable();
			}
		});

		nextBtn.addEventListener("click", () => {
			const totalPages = Math.ceil(filteredData.length / rowsPerPage);
			if (currentPage < totalPages) {
				currentPage++;
				renderTable();
			}
		});

		// Inicializar
		renderTable();
	</script>
</Layout>
